package example.modi.cls.pack1;

/*
 * default(package friendly) 제한자는 접근 제한자를 생략하면
 * 자동으로 간주됩니다. 같은 패키지 내부 자료들 끼리만 접근 가능합니다.
 * 클래스는 default 또는 public 만 사용 가능(private, protected 사용불가)
 */
class A {

}
